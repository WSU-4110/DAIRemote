# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Restore dependencies
      run: dotnet restore /p:EnableWindowsTargeting=true
      working-directory: DAIRemote
    - name: Build
      run: dotnet build --no-restore /p:EnableWindowsTargeting=true
      working-directory: DAIRemote
    - name: Build Tests
      run: dotnet build ./DAIRemote.tests/DAIRemote.tests.csproj
    - name: Test
      run: |
        if [[ "$RUNNER_OS" == "Windows" ]]; then
          dotnet test --no-build --verbosity normal --diagnostics /p:EnableWindowsTargeting=true
        fi
      working-directory: ./DAIRemote.tests


